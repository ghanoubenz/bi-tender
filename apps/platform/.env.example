# Local development. Never commit real secrets — see .env.example.
PAYLOAD_SECRET=
DATABASE_URI=
NEXT_PUBLIC_SERVER_URL=

# Tender AI Engine (Phase 2). Platform runs fine without it.
ENGINE_BASE_URL=
ENGINE_SERVICE_TOKEN=
