/**
 * Visible build stamp.
 *
 * Shown in the sidebar footer so anyone looking at a running copy can say
 * which version they are on. Bump this whenever a change should be visible to
 * the founder — it turns "I don't see the change" from a guessing game into a
 * one-second check.
 */
export const BUILD = 'build 7 · design pass'
